class Renderer:
    """
    Responsible for rendering frames in a given Order.

    Responsibilities:
    - Take an Order object and render frames sequentially.
    - Decide rendering strategy based on available media in each frame.
    - Handle different media types: video, audio, image, subtitles.

    Attributes:
        order (Order): The Order object containing frame sequence.
        output (str): Path to the final rendered video file.

    Methods:
        render(output="output.mp4"): Render the full sequence to the output file.
        render_frame(frame): Render a single frame, handling combinations
            of video/audio/image/subtitles appropriately.
    """

    def __init__(self):
        pass

    def render():
        pass